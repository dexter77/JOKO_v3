<h1 class="text-6xl text-rose-700 font-bold text-center mt-24">JOKO</h1>
<div class="flex flex-col flex-1 w-32 justify-center ">
  <div class="grid grid-cols-3 gap-4">
    <%= simple_form_for @user do |f| %>
          <%= f.input :email,
                      required: false,
                      autofocus: true,
                      input_html: { autocomplete: "email" } %>
          <%= f.input :password,
                      required: false,
                      input_html: { autocomplete: "current-password" } %>
          <%# <%= f.input :remember_me, class:"checke", as: :boolean if devise_mapping.rememberable? %>

        <div class="form-actions">
          <%= f.button :submit, "Connexion", class:'send-button' %>
        </div>
      <% end %>
      <br>
      <%# <%= render "devise/shared/links" %>
    </div>
  </div>

  <%= link_to "Crée ton compte", new_user_path, class: "btn btn-primary", id: "btn_signup" %>
</div>
